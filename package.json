{
  "name": "googlebooks-app",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start": "npm install",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && node server/server.js\"",
    "install": "cd server && npm i && cd ../client && npm i && npm run develop",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.1.0"
  },
  "dependencies": {
    "@apollo/client": "^3.5.9",
    "apollo-server-express": "^2.15.0"
  }
}
